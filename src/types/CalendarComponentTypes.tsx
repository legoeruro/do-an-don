export type DateInfo = {
    from: Date, 
    to: Date, 
}

export type TableHeaderInfo = { 
    fromText: string,
    toText: string
}

export type RowHeaderInfo = {
    breakfastText: string,
    lunchText: string,
    mainMealText: string,
    stirFryText: string,
    soupText: string
    snackText: string
}